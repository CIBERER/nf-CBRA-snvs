nextflow_workflow {

    name "Test Subworkflow CHECK_PROCESS_AUTOMAP"
    script "../main.nf"
    workflow "CHECK_PROCESS_AUTOMAP"

    tag "subworkflows"
    tag "subworkflows/check_process_automap"


    test("automap - vcf") {

        when {
            workflow {
                """
                input[0] = Channel.of([
                    [ id:'test'], // meta map
                    file("https://zenodo.org/records/15730236/files/NA12878-HG001.hg38.final.chr1.chr22.vcf.gz?download=1", checkIfExists: true)
                ])
                input[1] = "hg38"
                """
            }
        }
        then {
            assertAll(
                { assert workflow.success},
                { assert snapshot(workflow.out).match()}
            )
        }
    }

    test("automap not working - vcf") {

        when {
            workflow {
                """
                input[0] = Channel.of([
                    [ id:'test'], // meta map
                    file("https://zenodo.org/records/15355673/files/Sample1_T1.dragen.PASS.vcf.gz?download=1", checkIfExists: true)
                ])
                input[1] = "hg38"
                """
            }
        }
        then {
            assertAll(
                { assert workflow.success},
                { assert snapshot(workflow.out).match()}
            )
        }
    }
}