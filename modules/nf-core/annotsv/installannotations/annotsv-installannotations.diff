Changes in component 'nf-core/annotsv/installannotations'
'modules/nf-core/annotsv/installannotations/environment.yml' is unchanged
Changes in 'annotsv/installannotations/main.nf':
--- modules/nf-core/annotsv/installannotations/main.nf
+++ modules/nf-core/annotsv/installannotations/main.nf
@@ -9,7 +9,8 @@
 
     output:
     path "AnnotSV_annotations", emit: annotations
-    tuple val("${task.process}"), val('annotsv'), eval("AnnotSV --version | sed 's/AnnotSV //'"), emit: versions_annotsv, topic: versions
+    path "versions.yml"       , emit: versions
+    //tuple val("${task.process}"), val('annotsv'), eval("AnnotSV --version | sed 's/AnnotSV //'"), emit: versions_annotsv, topic: versions
 
     when:
     task.ext.when == null || task.ext.when
@@ -27,6 +28,12 @@
         echo "Install with tag 'v\$TAG' and '\$TAG' failed - falling back to 'master'" >&2
         INSTALL_annotations.sh master || { echo "Install with 'master' failed" >&2; exit 1; }
     fi
+
+    cat <<-END_VERSIONS > versions.yml
+    "${task.process}":
+        annotsv: \$(echo \$(AnnotSV --version | sed 's/AnnotSV //'))
+    END_VERSIONS
+
     """
 
     stub:

'modules/nf-core/annotsv/installannotations/meta.yml' is unchanged
'modules/nf-core/annotsv/installannotations/tests/main.nf.test' is unchanged
'modules/nf-core/annotsv/installannotations/tests/main.nf.test.snap' is unchanged
************************************************************
